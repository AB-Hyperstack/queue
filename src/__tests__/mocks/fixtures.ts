import type { Ticket, Queue, Organization, Subscription, SubscriptionStatusResponse } from '@/lib/types/database';

export const fixtures = {
  ticket: (overrides: Partial<Ticket> = {}): Ticket => ({
    id: 'ticket-1',
    queue_id: 'queue-1',
    org_id: 'org-1',
    ticket_number: 1,
    display_code: 'G-001',
    customer_name: 'John Doe',
    customer_phone: '+46701234567',
    status: 'waiting',
    position: 1,
    joined_at: new Date(Date.now() - 600000).toISOString(),
    called_at: null,
    served_at: null,
    push_subscription: null,
    metadata: {},
    created_at: new Date().toISOString(),
    ...overrides,
  }),

  servingTicket: (overrides: Partial<Ticket> = {}): Ticket => ({
    id: 'ticket-2',
    queue_id: 'queue-1',
    org_id: 'org-1',
    ticket_number: 2,
    display_code: 'G-002',
    customer_name: 'Jane Smith',
    customer_phone: null,
    status: 'serving',
    position: null,
    joined_at: new Date(Date.now() - 1200000).toISOString(),
    called_at: new Date(Date.now() - 300000).toISOString(),
    served_at: null,
    push_subscription: null,
    metadata: {},
    created_at: new Date().toISOString(),
    ...overrides,
  }),

  servedTicket: (overrides: Partial<Ticket> = {}): Ticket => ({
    id: 'ticket-3',
    queue_id: 'queue-1',
    org_id: 'org-1',
    ticket_number: 3,
    display_code: 'G-003',
    customer_name: 'Bob Wilson',
    customer_phone: null,
    status: 'served',
    position: null,
    joined_at: new Date(Date.now() - 1800000).toISOString(),
    called_at: new Date(Date.now() - 900000).toISOString(),
    served_at: new Date(Date.now() - 60000).toISOString(),
    push_subscription: null,
    metadata: {},
    created_at: new Date().toISOString(),
    ...overrides,
  }),

  queue: (overrides: Partial<Queue> = {}): Queue => ({
    id: 'queue-1',
    org_id: 'org-1',
    name: 'General',
    slug: 'general',
    color: '#3b82f6',
    is_active: true,
    avg_service_time_min: 5,
    created_at: new Date().toISOString(),
    ...overrides,
  }),

  organization: (overrides: Partial<Organization> = {}): Organization => ({
    id: 'org-1',
    name: 'Test Clinic',
    slug: 'test-clinic',
    settings: {},
    created_at: new Date().toISOString(),
    ...overrides,
  }),

  subscription: (overrides: Partial<Subscription> = {}): Subscription => ({
    id: 'sub-1',
    org_id: 'org-1',
    status: 'active',
    plan: 'monthly',
    trial_start: new Date(Date.now() - 86400000 * 14).toISOString(),
    trial_end: new Date().toISOString(),
    current_period_start: new Date().toISOString(),
    current_period_end: new Date(Date.now() + 86400000 * 30).toISOString(),
    stripe_customer_id: 'cus_test123',
    stripe_subscription_id: 'sub_test123',
    created_at: new Date().toISOString(),
    updated_at: new Date().toISOString(),
    ...overrides,
  }),

  subscriptionStatus: (overrides: Partial<SubscriptionStatusResponse> = {}): SubscriptionStatusResponse => ({
    status: 'active',
    plan: 'monthly',
    trial_start: new Date(Date.now() - 86400000 * 14).toISOString(),
    trial_end: new Date().toISOString(),
    trial_days_remaining: 0,
    current_period_start: new Date().toISOString(),
    current_period_end: new Date(Date.now() + 86400000 * 30).toISOString(),
    ...overrides,
  }),

  trialingSubscriptionStatus: (overrides: Partial<SubscriptionStatusResponse> = {}): SubscriptionStatusResponse => ({
    status: 'trialing',
    plan: null,
    trial_start: new Date(Date.now() - 86400000 * 7).toISOString(),
    trial_end: new Date(Date.now() + 86400000 * 7).toISOString(),
    trial_days_remaining: 7,
    current_period_start: null,
    current_period_end: null,
    ...overrides,
  }),

  expiredSubscriptionStatus: (overrides: Partial<SubscriptionStatusResponse> = {}): SubscriptionStatusResponse => ({
    status: 'expired',
    plan: null,
    trial_start: new Date(Date.now() - 86400000 * 28).toISOString(),
    trial_end: new Date(Date.now() - 86400000 * 14).toISOString(),
    trial_days_remaining: 0,
    current_period_start: null,
    current_period_end: null,
    ...overrides,
  }),
};
